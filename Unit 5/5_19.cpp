#include<bits/stdc++.h>
using namespace std;

int main()
{	string csp;
	do{
		string s1,s2;
		cin>>s1>>s2;
		cout<<(s1.size()<s2.size() ? s1:s20)<<endl;
		cin>>csp;
	}while(!csp.empty()&&csp=="y");
	return 0;
}